# packages/shared-core

Cross-platform shared logic (pure JS / framework-agnostic).

Will hold (when extracted from `platforms/x-project/`):
- i18n message dictionaries and translator
- Default + remote config schemas
- Subscription/quota state machine
- Storage adapter abstraction (Chrome storage / Capacitor Preferences)

For now: **empty placeholder**. Populate when TikTok-project is added and
real duplication pain appears.
