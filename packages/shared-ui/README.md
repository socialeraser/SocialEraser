# packages/shared-ui

Shared UI components (HTML/CSS snippets, design tokens, icon set).

For now: **empty placeholder**. Populate when a second platform wants to
reuse the same sidepanel/options UI.
