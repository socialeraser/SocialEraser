{
  "name": "social-eraser",
  "version": "1.0.0",
  "description": "SocialEraser - Batch Delete Twitter History",
  "main": "www/index.html",
  "scripts": {
    "sync": "node scripts/sync-shared.js",
    "start": "npm run build && npx cap run android",
    "build": "npm run sync && npx cap copy",
    "android:open": "npx cap open android",
    "test": "node scripts/run-verify.js",
    "verify": "node scripts/run-verify.js",
    "verify:single": "node scripts/run-verify.js --single"
  },
  "keywords": [
    "twitter",
    "delete",
    "batch",
    "cleanup"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^2.5.0",
    "@capacitor/cli": "^2.5.0",
    "@capacitor/core": "^2.5.0",
    "cordova-android": "^9.1.0"
  }
}
