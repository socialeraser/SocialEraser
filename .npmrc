# Intentionally empty.
# `prefix` and `cache` are configured in ~/.npmrc (user-level) so all
# projects (including Vue) share the same external-SSD locations.
# Newer npm versions forbid `prefix` in project-level .npmrc, which is
# why this file used to break `npm run build`.
